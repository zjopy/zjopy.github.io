{
  "scripts": {
    "build": "rm -r build & npx @11ty/eleventy",
    "watch": "rm -r build & npx @11ty/eleventy --watch",
    "dev": "rm -r build & npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.0.2"
  },
  "private": true
}
